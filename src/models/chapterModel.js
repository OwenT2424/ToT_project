// Server Block ------
const pool = require('../config/db');
const { v4: uuidv4 } = require('uuid');

const Chapter = {
  // Create functions here -> KRIS
};

// Server Block ------
module.exports = Chapter;
